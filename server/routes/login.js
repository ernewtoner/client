module.exports = (() => {
    const express = require('express');
    const router = express.Router();

    // API endpoint to get existing user from users table and authenticate the user
    // router.post('/', (req, res) => {
    //     // verify user exists with given email
    //     // return error if not; else proceed

    //     // verify given password is correct
    //     // return error if not; else send token/cookie
    // });

    return router;
})();