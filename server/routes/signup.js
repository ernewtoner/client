module.exports = (() => {
    const express = require('express');
    const router = express.Router();

    // API endpoint to create new user in users table
    // router.post('/', (req, res) => {
    //     // verify user with given email does not already exist
    //     // return error if so; else proceed
        
    //     // verify all fields are valid
    //     // return error if not; else send user token/cookie
    // });

    return router;
})();